# System Overview

React SPA memakai local state. Router browser berbasis History API mengelola route. Framer Motion memberi feedback singkat dan menghormati reduced motion. JSZip membuat archive browser. Tidak ada request server.

