# Security

Warning regex local untuk `api_key|token|password|secret` pada nama/content sementara. Value tidak ditampilkan, dipersist, dikirim, atau dimasukkan ZIP. Archive memakai whitelist dokumen demo.
