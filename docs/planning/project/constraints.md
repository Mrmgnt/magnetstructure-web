# Constraints

React, Vite, JavaScript, CSS, Framer Motion, browser routing, JSZip, Vercel SPA rewrite. Tanpa backend/auth/database/secret. Semantic HTML, label form, focus ring, aria-live, reduced motion. Layout usable pada 1440px, 1024px, 375px.
