# Test Strategy

1. Static: `npm run build` dan `npm run lint`.
2. Manual flow 8 route.
3. UI contract: IDs, state discovery, warning secret, responsive, reduced motion.
4. ZIP: buka dan cek whitelist.
5. Repo gate: status bersih dan diff check.
